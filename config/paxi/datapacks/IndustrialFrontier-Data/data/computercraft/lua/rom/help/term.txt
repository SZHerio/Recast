Функции API Terminal:
term.write( text )
term.blit( text, textColor, backgroundColor )
term.clear()
term.clearLine()
term.getCursorPos()
term.setCursorPos( x, y )
term.setCursorBlink( blink )
term.isColor()
term.setTextColor( color )
term.setBackgroundColor( color )
term.getTextColor()
term.getBackgroundColor()
term.getSize()
term.scroll( n )
term.redirect( object )
term.current()
term.setPaletteColor( color, r, g, b )
term.getPaletteColor( color )

События терминалов:
"term_resize" - размер терминала изменился. Это может произойти в многозадачной среде или когда программа "monitor" перенаправляет вывод терминала.
